console.error([
  "No current final blind set exists yet.",
  "Final-blind v2 was consumed when its inspected failures informed the new cloud architecture.",
  "Finish and freeze the production candidate, create an untouched v3, then wire this command to its immutable first run.",
].join("\n"));

process.exitCode = 1;
